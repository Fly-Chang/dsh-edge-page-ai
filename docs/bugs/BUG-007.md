# BUG-007 注入失败或面板关闭后 bootstrap 标志残留，再次点击书签无反应

- 状态: resolved
- 严重程度: S2 严重
- 发现时间: 2026-08-16 20:45 +08:00
- 发现人: 用户实测反馈（点击书签无反应）
- 处理人: edge-page-ai 开发
- 发现环境: Windows 11 / Edge / edge-page-ai 0.1.0-dev
- 关联功能/文件: `src/gateway/app.js`、`src/gateway/client/injected-page.mjs`

## 现象描述

书签地址已正确（不再跳转说明页），但在页面点击书签没有任何反应：
无面板、无错误提示。刷新页面后可能恢复。

## 复现步骤

1. 首次点击书签时网关未启动，或 `client.mjs` 加载失败。
2. 修复网关后，在同一页面再次点击书签。
3. 或者：正常打开面板后点击 × 关闭，再点击书签尝试重新打开。

## 实际结果

第二次点击完全无反应。

## 期望结果

每次点击都应重新注入，或给出可见的错误提示；失败后再次点击可以重试。

## 原因分析

书签代码与 `bootstrap.js` 在**实际加载脚本之前**就写入
`window.__DSH_BOOTSTRAPPED__ = true` 作为防重复注入标志：

1. 当 bootstrap/client 脚本加载失败时，标志没有被清除，后续点击直接命中
   `if(window.__DSH_BOOTSTRAPPED__)return;` 静默退出；
2. 面板关闭按钮只清除了 `window.__DSH_PANEL__`，没有清除 bootstrap 标志，
   关闭后无法再次用书签打开面板。

## 修复方案

1. 书签代码与 `bootstrap.js` 给注入的 `<script>` 增加 `onerror`：
   清除 `window.__DSH_BOOTSTRAPPED__` 并 `alert` 明确错误原因。
2. 页面客户端初始化包在 try/catch 中：失败时清除标志并提示。
3. 面板关闭时同时清除 `__DSH_PANEL__` 与 `__DSH_BOOTSTRAPPED__`，允许再次点击书签重开。
4. 集成测试新增断言：说明页与 bootstrap 均包含 `onerror` 与标志清除逻辑。

## 验证记录

| 日期 | 验证内容 | 结果 |
| --- | --- | --- |
| 2026-08-16 | `npm test` 18/18 通过（含新断言） | 通过 |
| 2026-08-16 | Edge headless 实际加载页面 → bootstrap → client，DOM 出现 `#dsh-page-ai-panel` | 通过 |
| 待用户复测 | 真实 Edge 中：关闭面板后再次点击书签应重新出现面板；网关关闭时点击应弹出错误提示 | 待验证 |

## 变更历史

| 日期 | 状态变更 | 说明 |
| --- | --- | --- |
| 2026-08-16 | 新建 | 用户反馈点击书签无反应，定位 bootstrap 标志残留 |
| 2026-08-16 | in-progress | 增加失败清理与可见错误提示 |
| 2026-08-16 | resolved | 自动化与 headless 冒烟通过，待用户复测 |
